diff --git a/obel.exe b/obel.exe
index 645ad9a..9cb921e 100644
Binary files a/obel.exe and b/obel.exe differ
diff --git a/src/obel/core.odin b/src/obel/core.odin
index 196c760..d271b34 100644
--- a/src/obel/core.odin
+++ b/src/obel/core.odin
@@ -8,7 +8,7 @@ import "core:os"
 import filepath "core:path/filepath"
 import "core:strings"
 
-// Native function binding =========================================================================
+// Host binding ===================================================================================
 
 new_native_function_object :: proc(native: NativeProc) -> ^NativeFunctionObject {
 	function := new(NativeFunctionObject)
@@ -28,6 +28,16 @@ bind_native_function :: proc(vm: ^VM, bindings: ^[dynamic]Binding, name: string,
 	})
 }
 
+bind_host_value :: proc(vm: ^VM, bindings: ^[dynamic]Binding, name: string, value: Value) {
+	symbol := intern_symbol(vm, name)
+
+	append(bindings, Binding{
+		symbol  = symbol,
+		value   = value,
+		mutable = false,
+	})
+}
+
 
 // Builtin binding ================================================================================
 
@@ -624,6 +634,21 @@ require_int_arg :: proc(args: []Value, index: int, proc_name, arg_name: string)
 	return value, true
 }
 
+require_number_arg :: proc(args: []Value, index: int, proc_name, arg_name: string) -> (f64, bool) {
+	int_value, is_int := args[index].(i64)
+	if is_int {
+		return f64(int_value), true
+	}
+
+	float_value, is_float := args[index].(f64)
+	if is_float {
+		return float_value, true
+	}
+
+	runtime_error(fmt.tprintf("`%s` expected number as %s argument.", proc_name, arg_name))
+	return 0, false
+}
+
 require_vector_arg :: proc(args: []Value, index: int, proc_name, arg_name: string) -> (^VectorObject, bool) {
 	object, is_object := args[index].(^Object)
 	if !is_object || object.kind != .VECTOR {
@@ -662,6 +687,17 @@ value_err_vector :: proc(value, err: Value) -> Value {
 	return Value(cast(^Object)new_vector_object(items))
 }
 
+F64_I64_MIN           :: -9223372036854775808.0
+F64_I64_MAX_EXCLUSIVE :: 9223372036854775808.0
+
+value_from_integral_float :: proc(value: f64) -> Value {
+	if value >= F64_I64_MIN && value < F64_I64_MAX_EXCLUSIVE {
+		return Value(i64(value))
+	}
+
+	return Value(value)
+}
+
 
 // Native builtins ================================================================================
 
@@ -1787,6 +1823,408 @@ install_host_module :: proc(vm: ^VM, id: string, exports: []Binding) {
 }
 
 
+// Math module ====================================================================================
+
+// (floor number) number; Round down, returning int when the result fits.
+native_math_floor :: proc(vm: ^VM, args: []Value) -> Value {
+	if !require_arg_count(args, 1, "`math/floor` expects one argument.\nusage: (math/floor number)") { return Value{} }
+
+	int_value, is_int := args[0].(i64)
+	if is_int { return Value(int_value) }
+
+	value, value_ok := require_number_arg(args, 0, "math/floor", "first")
+	if !value_ok { return Value{} }
+
+	return value_from_integral_float(math.floor(value))
+}
+
+// (ceil number) number; Round up, returning int when the result fits.
+native_math_ceil :: proc(vm: ^VM, args: []Value) -> Value {
+	if !require_arg_count(args, 1, "`math/ceil` expects one argument.\nusage: (math/ceil number)") { return Value{} }
+
+	int_value, is_int := args[0].(i64)
+	if is_int { return Value(int_value) }
+
+	value, value_ok := require_number_arg(args, 0, "math/ceil", "first")
+	if !value_ok { return Value{} }
+
+	return value_from_integral_float(math.ceil(value))
+}
+
+// (round number) number; Round to nearest, halves away from zero.
+native_math_round :: proc(vm: ^VM, args: []Value) -> Value {
+	if !require_arg_count(args, 1, "`math/round` expects one argument.\nusage: (math/round number)") { return Value{} }
+
+	int_value, is_int := args[0].(i64)
+	if is_int { return Value(int_value) }
+
+	value, value_ok := require_number_arg(args, 0, "math/round", "first")
+	if !value_ok { return Value{} }
+
+	return value_from_integral_float(math.round(value))
+}
+
+// (trunc number) number; Round toward zero, returning int when the result fits.
+native_math_trunc :: proc(vm: ^VM, args: []Value) -> Value {
+	if !require_arg_count(args, 1, "`math/trunc` expects one argument.\nusage: (math/trunc number)") { return Value{} }
+
+	int_value, is_int := args[0].(i64)
+	if is_int { return Value(int_value) }
+
+	value, value_ok := require_number_arg(args, 0, "math/trunc", "first")
+	if !value_ok { return Value{} }
+
+	return value_from_integral_float(math.trunc(value))
+}
+
+// (sqrt number) float; Square root.
+native_math_sqrt :: proc(vm: ^VM, args: []Value) -> Value {
+	if !require_arg_count(args, 1, "`math/sqrt` expects one argument.\nusage: (math/sqrt number)") { return Value{} }
+
+	value, value_ok := require_number_arg(args, 0, "math/sqrt", "first")
+	if !value_ok { return Value{} }
+
+	return Value(math.sqrt(value))
+}
+
+// (pow number number) float; Raise base to exponent.
+native_math_pow :: proc(vm: ^VM, args: []Value) -> Value {
+	if !require_arg_count(args, 2, "`math/pow` expects two arguments.\nusage: (math/pow base exponent)") { return Value{} }
+
+	base, base_ok := require_number_arg(args, 0, "math/pow", "first")
+	if !base_ok { return Value{} }
+	exponent, exponent_ok := require_number_arg(args, 1, "math/pow", "second")
+	if !exponent_ok { return Value{} }
+
+	return Value(math.pow(base, exponent))
+}
+
+// (exp number) float; e raised to x.
+native_math_exp :: proc(vm: ^VM, args: []Value) -> Value {
+	if !require_arg_count(args, 1, "`math/exp` expects one argument.\nusage: (math/exp number)") { return Value{} }
+
+	value, value_ok := require_number_arg(args, 0, "math/exp", "first")
+	if !value_ok { return Value{} }
+
+	return Value(math.exp(value))
+}
+
+// (log number) float; Natural logarithm.
+native_math_log :: proc(vm: ^VM, args: []Value) -> Value {
+	if !require_arg_count(args, 1, "`math/log` expects one argument.\nusage: (math/log number)") { return Value{} }
+
+	value, value_ok := require_number_arg(args, 0, "math/log", "first")
+	if !value_ok { return Value{} }
+
+	return Value(math.ln(value))
+}
+
+// (sin radians) float; Sine.
+native_math_sin :: proc(vm: ^VM, args: []Value) -> Value {
+	if !require_arg_count(args, 1, "`math/sin` expects one argument.\nusage: (math/sin radians)") { return Value{} }
+
+	value, value_ok := require_number_arg(args, 0, "math/sin", "first")
+	if !value_ok { return Value{} }
+
+	return Value(math.sin(value))
+}
+
+// (cos radians) float; Cosine.
+native_math_cos :: proc(vm: ^VM, args: []Value) -> Value {
+	if !require_arg_count(args, 1, "`math/cos` expects one argument.\nusage: (math/cos radians)") { return Value{} }
+
+	value, value_ok := require_number_arg(args, 0, "math/cos", "first")
+	if !value_ok { return Value{} }
+
+	return Value(math.cos(value))
+}
+
+// (tan radians) float; Tangent.
+native_math_tan :: proc(vm: ^VM, args: []Value) -> Value {
+	if !require_arg_count(args, 1, "`math/tan` expects one argument.\nusage: (math/tan radians)") { return Value{} }
+
+	value, value_ok := require_number_arg(args, 0, "math/tan", "first")
+	if !value_ok { return Value{} }
+
+	return Value(math.tan(value))
+}
+
+// (asin number) float; Arc sine.
+native_math_asin :: proc(vm: ^VM, args: []Value) -> Value {
+	if !require_arg_count(args, 1, "`math/asin` expects one argument.\nusage: (math/asin number)") { return Value{} }
+
+	value, value_ok := require_number_arg(args, 0, "math/asin", "first")
+	if !value_ok { return Value{} }
+
+	return Value(math.asin(value))
+}
+
+// (acos number) float; Arc cosine.
+native_math_acos :: proc(vm: ^VM, args: []Value) -> Value {
+	if !require_arg_count(args, 1, "`math/acos` expects one argument.\nusage: (math/acos number)") { return Value{} }
+
+	value, value_ok := require_number_arg(args, 0, "math/acos", "first")
+	if !value_ok { return Value{} }
+
+	return Value(math.acos(value))
+}
+
+// (atan number) float; Arc tangent.
+native_math_atan :: proc(vm: ^VM, args: []Value) -> Value {
+	if !require_arg_count(args, 1, "`math/atan` expects one argument.\nusage: (math/atan number)") { return Value{} }
+
+	value, value_ok := require_number_arg(args, 0, "math/atan", "first")
+	if !value_ok { return Value{} }
+
+	return Value(math.atan(value))
+}
+
+// (atan2 y x) float; Angle of a direction.
+native_math_atan2 :: proc(vm: ^VM, args: []Value) -> Value {
+	if !require_arg_count(args, 2, "`math/atan2` expects two arguments.\nusage: (math/atan2 y x)") { return Value{} }
+
+	y, y_ok := require_number_arg(args, 0, "math/atan2", "first")
+	if !y_ok { return Value{} }
+	x, x_ok := require_number_arg(args, 1, "math/atan2", "second")
+	if !x_ok { return Value{} }
+
+	return Value(math.atan2(y, x))
+}
+
+// (radians degrees) float; Convert degrees to radians.
+native_math_radians :: proc(vm: ^VM, args: []Value) -> Value {
+	if !require_arg_count(args, 1, "`math/radians` expects one argument.\nusage: (math/radians degrees)") { return Value{} }
+
+	value, value_ok := require_number_arg(args, 0, "math/radians", "first")
+	if !value_ok { return Value{} }
+
+	return Value(math.to_radians(value))
+}
+
+// (degrees radians) float; Convert radians to degrees.
+native_math_degrees :: proc(vm: ^VM, args: []Value) -> Value {
+	if !require_arg_count(args, 1, "`math/degrees` expects one argument.\nusage: (math/degrees radians)") { return Value{} }
+
+	value, value_ok := require_number_arg(args, 0, "math/degrees", "first")
+	if !value_ok { return Value{} }
+
+	return Value(math.to_degrees(value))
+}
+
+// (finite? number) bool; True for ints and finite floats.
+native_math_finite_predicate :: proc(vm: ^VM, args: []Value) -> Value {
+	if !require_arg_count(args, 1, "`math/finite?` expects one argument.\nusage: (math/finite? number)") { return Value{} }
+
+	_, is_int := args[0].(i64)
+	if is_int { return Value(bool(true)) }
+
+	value, is_float := args[0].(f64)
+	if is_float {
+		return Value(bool(!math.is_nan(value) && !math.is_inf(value)))
+	}
+
+	runtime_error("`math/finite?` expected number as argument.")
+	return Value{}
+}
+
+// (nan? number) bool; True only for NaN floats.
+native_math_nan_predicate :: proc(vm: ^VM, args: []Value) -> Value {
+	if !require_arg_count(args, 1, "`math/nan?` expects one argument.\nusage: (math/nan? number)") { return Value{} }
+
+	_, is_int := args[0].(i64)
+	if is_int { return Value(bool(false)) }
+
+	value, is_float := args[0].(f64)
+	if is_float {
+		return Value(bool(math.is_nan(value)))
+	}
+
+	runtime_error("`math/nan?` expected number as argument.")
+	return Value{}
+}
+
+// (inf? number) bool; True only for positive or negative infinity.
+native_math_inf_predicate :: proc(vm: ^VM, args: []Value) -> Value {
+	if !require_arg_count(args, 1, "`math/inf?` expects one argument.\nusage: (math/inf? number)") { return Value{} }
+
+	_, is_int := args[0].(i64)
+	if is_int { return Value(bool(false)) }
+
+	value, is_float := args[0].(f64)
+	if is_float {
+		return Value(bool(math.is_inf(value)))
+	}
+
+	runtime_error("`math/inf?` expected number as argument.")
+	return Value{}
+}
+
+// (sign number) int; -1, 0, or 1 by numeric sign.
+native_math_sign :: proc(vm: ^VM, args: []Value) -> Value {
+	if !require_arg_count(args, 1, "`math/sign` expects one argument.\nusage: (math/sign number)") { return Value{} }
+
+	int_value, is_int := args[0].(i64)
+	if is_int {
+		if int_value < 0 { return Value(i64(-1)) }
+		if int_value > 0 { return Value(i64(1)) }
+		return Value(i64(0))
+	}
+
+	value, is_float := args[0].(f64)
+	if is_float {
+		if math.is_nan(value) {
+			runtime_error("`math/sign` cannot classify NaN.")
+			return Value{}
+		}
+		if value < 0 { return Value(i64(-1)) }
+		if value > 0 { return Value(i64(1)) }
+		return Value(i64(0))
+	}
+
+	runtime_error("`math/sign` expected number as argument.")
+	return Value{}
+}
+
+// (fract number) float; Fractional part as x - floor(x).
+native_math_fract :: proc(vm: ^VM, args: []Value) -> Value {
+	if !require_arg_count(args, 1, "`math/fract` expects one argument.\nusage: (math/fract number)") { return Value{} }
+
+	_, is_int := args[0].(i64)
+	if is_int { return Value(f64(0)) }
+
+	value, value_ok := require_number_arg(args, 0, "math/fract", "first")
+	if !value_ok { return Value{} }
+
+	return Value(value - math.floor(value))
+}
+
+// (hypot x y) float; Length of the x/y vector.
+native_math_hypot :: proc(vm: ^VM, args: []Value) -> Value {
+	if !require_arg_count(args, 2, "`math/hypot` expects two arguments.\nusage: (math/hypot x y)") { return Value{} }
+
+	x, x_ok := require_number_arg(args, 0, "math/hypot", "first")
+	if !x_ok { return Value{} }
+	y, y_ok := require_number_arg(args, 1, "math/hypot", "second")
+	if !y_ok { return Value{} }
+
+	return Value(math.hypot(x, y))
+}
+
+// (lerp a b t) float; Linear interpolation.
+native_math_lerp :: proc(vm: ^VM, args: []Value) -> Value {
+	if !require_arg_count(args, 3, "`math/lerp` expects three arguments.\nusage: (math/lerp a b t)") { return Value{} }
+
+	a, a_ok := require_number_arg(args, 0, "math/lerp", "first")
+	if !a_ok { return Value{} }
+	b, b_ok := require_number_arg(args, 1, "math/lerp", "second")
+	if !b_ok { return Value{} }
+	t, t_ok := require_number_arg(args, 2, "math/lerp", "third")
+	if !t_ok { return Value{} }
+
+	return Value(math.lerp(a, b, t))
+}
+
+// (inverse-lerp a b x) float; Position of x between a and b.
+native_math_inverse_lerp :: proc(vm: ^VM, args: []Value) -> Value {
+	if !require_arg_count(args, 3, "`math/inverse-lerp` expects three arguments.\nusage: (math/inverse-lerp a b x)") { return Value{} }
+
+	a, a_ok := require_number_arg(args, 0, "math/inverse-lerp", "first")
+	if !a_ok { return Value{} }
+	b, b_ok := require_number_arg(args, 1, "math/inverse-lerp", "second")
+	if !b_ok { return Value{} }
+	x, x_ok := require_number_arg(args, 2, "math/inverse-lerp", "third")
+	if !x_ok { return Value{} }
+
+	if a == b {
+		runtime_error("`math/inverse-lerp` range cannot be zero.")
+		return Value{}
+	}
+
+	return Value(math.unlerp(a, b, x))
+}
+
+// (remap x in-min in-max out-min out-max) float; Map x from one range to another.
+native_math_remap :: proc(vm: ^VM, args: []Value) -> Value {
+	if !require_arg_count(args, 5, "`math/remap` expects five arguments.\nusage: (math/remap x in-min in-max out-min out-max)") { return Value{} }
+
+	x, x_ok := require_number_arg(args, 0, "math/remap", "first")
+	if !x_ok { return Value{} }
+	in_min, in_min_ok := require_number_arg(args, 1, "math/remap", "second")
+	if !in_min_ok { return Value{} }
+	in_max, in_max_ok := require_number_arg(args, 2, "math/remap", "third")
+	if !in_max_ok { return Value{} }
+	out_min, out_min_ok := require_number_arg(args, 3, "math/remap", "fourth")
+	if !out_min_ok { return Value{} }
+	out_max, out_max_ok := require_number_arg(args, 4, "math/remap", "fifth")
+	if !out_max_ok { return Value{} }
+
+	if in_min == in_max {
+		runtime_error("`math/remap` input range cannot be zero.")
+		return Value{}
+	}
+
+	return Value(math.remap(x, in_min, in_max, out_min, out_max))
+}
+
+// (wrap x min max) number; Wrap x into the half-open range [min, max).
+native_math_wrap :: proc(vm: ^VM, args: []Value) -> Value {
+	if !require_arg_count(args, 3, "`math/wrap` expects three arguments.\nusage: (math/wrap x min max)") { return Value{} }
+
+	x_int, x_is_int := args[0].(i64)
+	min_int, min_is_int := args[1].(i64)
+	max_int, max_is_int := args[2].(i64)
+	if x_is_int && min_is_int && max_is_int {
+		if min_int >= max_int {
+			runtime_error("`math/wrap` min must be less than max.")
+			return Value{}
+		}
+
+		width := i128(max_int) - i128(min_int)
+		offset := i128(x_int) - i128(min_int)
+		wrapped := offset % width
+		if wrapped < 0 {
+			wrapped += width
+		}
+
+		return Value(i64(i128(min_int) + wrapped))
+	}
+
+	x, x_ok := require_number_arg(args, 0, "math/wrap", "first")
+	if !x_ok { return Value{} }
+	min_value, min_ok := require_number_arg(args, 1, "math/wrap", "second")
+	if !min_ok { return Value{} }
+	max_value, max_ok := require_number_arg(args, 2, "math/wrap", "third")
+	if !max_ok { return Value{} }
+
+	if min_value >= max_value {
+		runtime_error("`math/wrap` min must be less than max.")
+		return Value{}
+	}
+
+	width := max_value - min_value
+	return Value(min_value + math.wrap(x - min_value, width))
+}
+
+// (smoothstep edge-a edge-b x) float; Clamped smooth transition from 0.0 to 1.0.
+native_math_smoothstep :: proc(vm: ^VM, args: []Value) -> Value {
+	if !require_arg_count(args, 3, "`math/smoothstep` expects three arguments.\nusage: (math/smoothstep edge-a edge-b x)") { return Value{} }
+
+	edge_a, edge_a_ok := require_number_arg(args, 0, "math/smoothstep", "first")
+	if !edge_a_ok { return Value{} }
+	edge_b, edge_b_ok := require_number_arg(args, 1, "math/smoothstep", "second")
+	if !edge_b_ok { return Value{} }
+	x, x_ok := require_number_arg(args, 2, "math/smoothstep", "third")
+	if !x_ok { return Value{} }
+
+	if edge_a >= edge_b {
+		runtime_error("`math/smoothstep` first edge must be less than second edge.")
+		return Value{}
+	}
+
+	return Value(math.smoothstep(edge_a, edge_b, x))
+}
+
+
 // String module ==================================================================================
 
 // (has? text part) bool; true if text contains part.
@@ -2583,6 +3021,46 @@ install_core_modules :: proc(vm: ^VM) {
 	bind_native_function(vm, &path_exports, "abs", native_path_abs)
 	install_host_module(vm, "path", path_exports[:])
 
+	// math
+	math_exports := make([dynamic]Binding)
+	defer delete(math_exports)
+
+	bind_host_value(vm, &math_exports, "pi", Value(f64(math.PI)))
+	bind_host_value(vm, &math_exports, "tau", Value(f64(math.TAU)))
+	bind_host_value(vm, &math_exports, "e", Value(f64(math.E)))
+	bind_host_value(vm, &math_exports, "inf", Value(math.inf_f64(1)))
+	bind_host_value(vm, &math_exports, "-inf", Value(math.inf_f64(-1)))
+	bind_host_value(vm, &math_exports, "nan", Value(math.nan_f64()))
+	bind_native_function(vm, &math_exports, "floor", native_math_floor)
+	bind_native_function(vm, &math_exports, "ceil", native_math_ceil)
+	bind_native_function(vm, &math_exports, "round", native_math_round)
+	bind_native_function(vm, &math_exports, "trunc", native_math_trunc)
+	bind_native_function(vm, &math_exports, "sqrt", native_math_sqrt)
+	bind_native_function(vm, &math_exports, "pow", native_math_pow)
+	bind_native_function(vm, &math_exports, "exp", native_math_exp)
+	bind_native_function(vm, &math_exports, "log", native_math_log)
+	bind_native_function(vm, &math_exports, "sin", native_math_sin)
+	bind_native_function(vm, &math_exports, "cos", native_math_cos)
+	bind_native_function(vm, &math_exports, "tan", native_math_tan)
+	bind_native_function(vm, &math_exports, "asin", native_math_asin)
+	bind_native_function(vm, &math_exports, "acos", native_math_acos)
+	bind_native_function(vm, &math_exports, "atan", native_math_atan)
+	bind_native_function(vm, &math_exports, "atan2", native_math_atan2)
+	bind_native_function(vm, &math_exports, "radians", native_math_radians)
+	bind_native_function(vm, &math_exports, "degrees", native_math_degrees)
+	bind_native_function(vm, &math_exports, "finite?", native_math_finite_predicate)
+	bind_native_function(vm, &math_exports, "nan?", native_math_nan_predicate)
+	bind_native_function(vm, &math_exports, "inf?", native_math_inf_predicate)
+	bind_native_function(vm, &math_exports, "sign", native_math_sign)
+	bind_native_function(vm, &math_exports, "fract", native_math_fract)
+	bind_native_function(vm, &math_exports, "hypot", native_math_hypot)
+	bind_native_function(vm, &math_exports, "lerp", native_math_lerp)
+	bind_native_function(vm, &math_exports, "inverse-lerp", native_math_inverse_lerp)
+	bind_native_function(vm, &math_exports, "remap", native_math_remap)
+	bind_native_function(vm, &math_exports, "wrap", native_math_wrap)
+	bind_native_function(vm, &math_exports, "smoothstep", native_math_smoothstep)
+	install_host_module(vm, "math", math_exports[:])
+
 	// os
 	os_exports := make([dynamic]Binding)
 	defer delete(os_exports)
